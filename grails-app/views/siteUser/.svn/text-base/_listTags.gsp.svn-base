<table>
     <tbody>
       <g:each in="${tagList}" var='tag'>
			<tr>
            	<td><g:link controller="brag" action="searchByTagForUser" params="['user':user.id,
tagName:tag.name]">${tag.name}</g:link></td>
			</tr>
       </g:each>
     </tbody>
</table>
